apiVersion: v1
kind: Pod
metadata:
  name: voting-pod
  labels:
    app: voting
spec:
  containers:
  - name: container-vote
    image: kodekloud/examplevotingapp_vote:v1
    ports:
    - containerPort: 80

